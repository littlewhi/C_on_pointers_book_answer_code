#ifndef _A_H_
#define _A_H_

#define MAX(a,b) ((a) > (b) ? (a) : (b))
#define MIN(a,b) ((a) < (b) ? (a) : (b))

#include <stdio.h>
#include <string.h>
#include <string.h>
#include <stdarg.h>
#include <ctype.h>
#include <limits.h>
#include <stdlib.h>

void a1( void );
void a2( void );
void a3( void );
void a4( void );
void a5( void );
void a6( void );

#endif